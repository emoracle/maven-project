pipeline {
    agent any

 tools {
        maven 'localMaven'
        docker 'localDocker'
    }

    stages{
        stage('build') {
            steps {
                bat 'mvn clean package'
                bat 'docker build . -t tomcatwebapp:${env.BUILD_ID}' 
            }
        }
    }
}